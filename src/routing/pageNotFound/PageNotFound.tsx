import React from 'react';
import { Typography } from '@mui/material';

/**
 * General pageNotFound screen
 */
export const PageNotFound: React.FC = () => {
    return (
        <Typography variant={'h4'}>
            Page not found!
        </Typography>
    );
};
